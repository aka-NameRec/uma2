# Code Review - UMA Project
**Дата:** 2026-01-13  
**Версия:** 2.3

## Executive Summary

Проект UMA представляет собой хорошо структурированную библиотеку для унифицированного доступа к данным через JSON-SQL запросы. Однако обнаружены существенные проблемы с соблюдением принципов DRY и SOLID, а также архитектурные недочеты, которые могут повлиять на поддерживаемость и масштабируемость кода.

## 🔴 Критические проблемы

### 1. Нарушение DRY в API Layer (`api.py`)

**Проблема:** Функции `uma_read`, `uma_save`, `uma_delete`, `uma_entity_details` содержат практически идентичную логику:

```python
# Повторяющийся паттерн в каждой функции:
entity = parse_entity_name(entity_name)
if namespace and not entity.namespace:
    entity = EntityName(entity=entity.entity, namespace=namespace)
context = _create_context(entity.namespace, user_context)
_check_access(str(entity), Operation.XXX, context)
handler = await get_registry().get_handler(entity, context)
return await handler.xxx(entity, ...)
```

**Решение:** Создать декоратор или базовую функцию для обработки общей логики:

```python
async def _execute_entity_operation(
    entity_name: str,
    operation: Operation,
    handler_method: str,
    namespace: str | None = None,
    user_context: Any = None,
    **kwargs: Any
) -> Any:
    """Common logic for entity operations."""
    entity = parse_entity_name(entity_name)
    if namespace and not entity.namespace:
        entity = EntityName(entity=entity.entity, namespace=namespace)
    
    context = _create_context(entity.namespace, user_context)
    _check_access(str(entity), operation, context)
    
    handler = await get_registry().get_handler(entity, context)
    method = getattr(handler, handler_method)
    return await method(entity, context, **kwargs)
```

**Приоритет:** 🔴 Высокий  
**Оценка трудозатрат:** 2-3 часа

---

### 2. Глобальное состояние (Anti-pattern)

**Проблема:** Множественное использование глобальных переменных:
- `api.py`: `_registry`
- `namespace_config.py`: `_namespace_configs`, `_default_namespace`
- `registry.py`: `_global_registry`

Это создает проблемы:
- Сложность тестирования (нужно сбрасывать состояние между тестами)
- Невозможность изолировать инстансы для разных целей
- Потенциальные race conditions в многопоточной среде

**Решение:** Использовать контейнер зависимостей или контекстный менеджер:

```python
@dataclass
class UMAApplication:
    """UMA application instance with all dependencies."""
    registry: EntityRegistry
    namespace_configs: Mapping[str, NamespaceConfig]
    default_namespace: str | None
    
    async def read(self, entity_name: str, ...) -> dict:
        """Read operation."""
        ...
    
    # ... other operations

# Usage:
uma = UMAApplication(
    registry=EntityRegistry(...),
    namespace_configs={...},
    default_namespace='main'
)
result = await uma.read('users', id=1)
```

**Приоритет:** 🔴 Высокий  
**Оценка трудозатрат:** 8-12 часов (breaking change)

---

### 3. Нарушение SRP: Гигантский `converter.py` (1033 строки)

**Проблема:** Файл содержит слишком много ответственностей:
1. JSQL → SQL конверсию
2. SQL → JSQL конверсию
3. Обработку всех типов операторов
4. Обработку JOIN'ов
5. Обработку функций
6. Обработку условий

**Решение:** Разбить на модули:

```
jsql/converter/
├── __init__.py
├── jsql_to_sql.py          # JSQL → SQL
├── sql_to_jsql.py          # SQL → JSQL
├── operators.py            # Операторы (comparison, logical, arithmetic)
├── expressions.py          # Выражения
├── conditions.py           # Условия
├── joins.py                # JOIN'ы
└── functions.py            # Функции
```

**Приоритет:** 🔴 Высокий  
**Оценка трудозатрат:** 6-8 часов

---

## 🟠 Важные проблемы

### 4. Нарушение SRP: `DefaultMetadataProvider` (286 строк)

**Проблема:** Класс отвечает за:
1. Управление кэшем метаданных
2. Рефлексию БД
3. Проверку доступа (метод `can`)
4. Получение таблиц
5. Список сущностей

**Решение:** Разделить на несколько компонентов:

```python
class MetadataCache:
    """Cache for metadata and tables."""
    ...

class DatabaseReflector:
    """Reflects database schema."""
    ...

class AccessController:
    """Controls access to operations."""
    ...

class DefaultMetadataProvider:
    """Coordinates metadata operations."""
    def __init__(self, cache: MetadataCache, reflector: DatabaseReflector):
        ...
```

**Приоритет:** 🟠 Средний  
**Оценка трудозатрат:** 4-6 часов

---

### 5. Неэффективный поиск колонок в `parser.py`

**Проблема:** В методе `_resolve_column` (строки 386-394):

```python
# Линейный поиск по всем таблицам
for table in self.table_aliases.values():
    if hasattr(table, 'columns') and field_spec in table.columns:
        return table.columns[field_spec]
```

При большом количестве JOIN'ов это O(n*m), где n - количество таблиц, m - количество колонок.

**Решение:** Создать плоский индекс колонок:

```python
def __init__(self, context: UMAContext) -> None:
    self.context = context
    self.ctes: dict[str, Select] = {}
    self.table_aliases: dict[str, Any] = {}
    self.select_aliases: dict[str, ColumnElement] = {}
    # Добавить индекс для быстрого поиска
    self._column_index: dict[str, list[tuple[str, ColumnElement]]] = {}

def _register_table_alias(self, alias_name: str, table: Any, ...) -> None:
    self.table_aliases[alias_name] = table
    # Индексируем колонки при регистрации таблицы
    if hasattr(table, 'columns'):
        for col in table.columns:
            if col.name not in self._column_index:
                self._column_index[col.name] = []
            self._column_index[col.name].append((alias_name, col))

async def _resolve_column(self, field_spec: str, from_clause: Any) -> Column:
    # Быстрый поиск через индекс - O(1)
    if candidates := self._column_index.get(field_spec):
        if len(candidates) == 1:
            return candidates[0][1]
        # Если несколько - нужна квалификация
        raise JSQLSyntaxError(f'Ambiguous column "{field_spec}"...')
```

**Приоритет:** 🟠 Средний  
**Оценка трудозатрат:** 3-4 часа

---

### 6. Дублирование логики обработки алиасов

**Проблема:** В `converter.py` и `parser.py` повторяется логика обработки алиасов таблиц и колонок.

**Локации:**
- `converter.py:568-570`: обработка алиасов для JOIN
- `converter.py:640-646`: обработка алиасов для FROM
- `parser.py:451-457`: обработка алиасов для JOIN
- `parser.py:176-187`: регистрация алиасов

**Решение:** Создать утилитарный класс `AliasManager`:

```python
class AliasManager:
    """Manages table and column aliases."""
    
    def __init__(self) -> None:
        self._table_aliases: dict[str, Any] = {}
        self._column_index: dict[str, list[tuple[str, ColumnElement]]] = {}
    
    def register_table(self, alias: str, table: Any) -> None:
        """Register table with alias."""
        ...
    
    def resolve_column(self, column_name: str, table_name: str | None = None) -> ColumnElement:
        """Resolve column by name, optionally qualified with table."""
        ...
```

**Приоритет:** 🟠 Средний  
**Оценка трудозатрат:** 2-3 часа

---

## 🟡 Рекомендации к улучшению

### 7. Избыточная проверка типов и валидация

**Проблема:** В `converter.py` множество проверок типов:

```python
if not isinstance(from_spec, str):
    raise JSQLSyntaxError(...)

if not isinstance(cond_spec, dict):
    raise InvalidExpressionError(...)

if not isinstance(join_spec, dict):
    raise InvalidExpressionError(...)
```

**Решение:** Использовать Pydantic models для валидации:

```python
from pydantic import BaseModel, Field, validator

class JSQLSelect(BaseModel):
    field: str | None = None
    alias: str | None = None
    func: str | None = None
    value: Any = None
    
    @validator('*', pre=True)
    def validate_one_of(cls, v, values):
        # Проверить что задано только одно поле
        ...

class JSQLQuery(BaseModel):
    from_: str = Field(..., alias='from')
    select: list[JSQLSelect] = Field(default_factory=lambda: [JSQLSelect(field='*')])
    where: JSQLCondition | None = None
    # ...
```

**Приоритет:** 🟡 Низкий  
**Оценка трудозатрат:** 6-8 часов

---

### 8. Отсутствие кэширования скомпилированных запросов

**Проблема:** Каждый JSQL запрос парсится заново, даже если это тот же запрос с разными параметрами.

**Решение:** Добавить кэширование парсинга:

```python
from functools import lru_cache

class JSQLParser:
    def __init__(self, context: UMAContext) -> None:
        self.context = context
        self._parse_cache: dict[str, Select] = {}
    
    async def parse(self, jsql: JSQLQuery, params: dict | None = None) -> Select:
        # Создать ключ кэша (без параметров)
        cache_key = self._make_cache_key(jsql)
        
        if cache_key in self._parse_cache:
            # Вернуть закэшированный запрос с подставленными параметрами
            return self._bind_params(self._parse_cache[cache_key], params)
        
        # Парсить и закэшировать
        query = await self._build_query(jsql)
        self._parse_cache[cache_key] = query
        return self._bind_params(query, params)
```

**Приоритет:** 🟡 Низкий  
**Оценка трудозатрат:** 4-5 часов

---

### 9. Циркулярные импорты

**Проблема:** В коде используются импорты внутри функций для избежания циркулярных зависимостей:

**Локации:**
- `registry.py:26-29`: импорт `DefaultEntityHandler` внутри `__init__`
- `utils.py:76`: импорт `get_registry` внутри `copy_field_meta`
- `parser.py:242`: импорт `get_table` внутри `_get_table_async`

**Решение:** Реорганизовать структуру модулей:

```
core/
├── types.py          # Все типы и протоколы (без зависимостей)
├── exceptions.py     # Исключения (без зависимостей)
├── context.py        # Контекст (зависит от types)
└── utils.py          # Утилиты (зависит от types, context)

handlers/
├── base.py           # DefaultEntityHandler (зависит от core)
└── virtual.py        # VirtualViewHandler (зависит от core)

registry.py           # EntityRegistry (зависит от handlers)
metadata.py           # MetadataProvider (зависит от core)
api.py                # API (зависит от всего)
```

**Приоритет:** 🟡 Низкий  
**Оценка трудозатрат:** 3-4 часа

---

### 10. Отсутствие паттерна Builder

**Проблема:** Создание сложных объектов (особенно `UMAContext`, `JSQLParser`) происходит напрямую, что усложняет тестирование.

**Решение:** Использовать Builder pattern:

```python
class UMAContextBuilder:
    """Builder for UMAContext."""
    
    def __init__(self) -> None:
        self._engine: Engine | None = None
        self._metadata_provider: MetadataProvider | None = None
        self._namespace: str = 'default'
        self._user_context: Any = None
        self._extra: dict[str, Any] = {}
    
    def with_engine(self, engine: Engine) -> 'UMAContextBuilder':
        self._engine = engine
        return self
    
    def with_metadata_provider(self, provider: MetadataProvider) -> 'UMAContextBuilder':
        self._metadata_provider = provider
        return self
    
    def build(self) -> UMAContext:
        if not self._engine:
            raise ValueError('Engine is required')
        if not self._metadata_provider:
            raise ValueError('MetadataProvider is required')
        
        return UMAContext(
            engine=self._engine,
            metadata_provider=self._metadata_provider,
            namespace=self._namespace,
            user_context=self._user_context,
            extra=self._extra
        )
```

**Приоритет:** 🟡 Низкий  
**Оценка трудозатрат:** 2-3 часа

---

## 🟢 Положительные моменты

1. ✅ **Хорошая типизация** - везде используются type hints
2. ✅ **Использование dataclass** - `EntityName`, `NamespaceConfig`, `UMAContext`
3. ✅ **Протоколы вместо ABC** - `EntityHandler`, `MetadataProvider`
4. ✅ **Использование match-case** - современный синтаксис Python 3.10+
5. ✅ **Walrus operator** - эффективное использование в условиях
6. ✅ **Async/await** - правильное использование асинхронности
7. ✅ **Docstrings** - хорошая документация функций
8. ✅ **Константы** - использование Enum для операторов и типов
9. ✅ **Proper exception hierarchy** - структурированные исключения

---

## Детальный анализ по модулям

### `api.py` (412 строк)
- ❌ Нарушение DRY (повторяющаяся логика)
- ❌ Глобальное состояние (`_registry`)
- ❌ Смешение ответственностей (парсинг, валидация, доступ, выполнение)
- ⚠️ Функция `_check_access` использует `hasattr` вместо `isinstance`

**Рекомендация:** Рефакторинг с использованием декоратора или базовой функции + удаление глобального состояния.

---

### `converter.py` (1033 строки)
- ❌ Нарушение SRP (слишком много ответственностей)
- ❌ Дублирование логики обработки ошибок
- ❌ Множественные проходы по спискам (неэффективно)
- ⚠️ Логирование на каждом шаге (может влиять на производительность)
- ⚠️ Отсутствие валидации входных данных (стоит использовать Pydantic)

**Рекомендация:** Разбить на отдельные модули по типам операций.

---

### `parser.py` (915 строк)
- ❌ Неэффективный поиск колонок (O(n*m))
- ❌ Дублирование логики регистрации алиасов
- ⚠️ Длинные методы (`_build_query`, `_build_select`, `_process_joins_for_aliases`)
- ⚠️ Циркулярный импорт (`get_table`)

**Рекомендация:** Создать индекс колонок, вынести логику алиасов в отдельный класс.

---

### `metadata.py` (286 строк)
- ❌ Нарушение SRP (кэш + рефлексия + доступ)
- ⚠️ Метод `can` слишком примитивен (всегда возвращает `True`)
- ⚠️ Кэш не имеет ограничения по размеру (может расти бесконечно)

**Рекомендация:** Разделить на компоненты, добавить LRU-кэш.

---

### `registry.py` (154 строки)
- ❌ Глобальное состояние (`_global_registry`)
- ⚠️ Метод `get_handler` возвращает класс, а не инстанс (потенциально сбивает с толку)

**Рекомендация:** Удалить глобальное состояние, рассмотреть использование инстансов вместо классов.

---

### `handlers/base.py` (279 строк)
- ✅ Хороший пример использования DRY (`_ensure_exists`, `_build_pk_condition`)
- ✅ Четкое разделение ответственностей в методах
- ⚠️ Метод `save` определяет create/update по наличию ID (может быть неочевидно)

**Рекомендация:** Рассмотреть явное разделение `create` и `update` методов.

---

### `handlers/virtual.py` (140 строк)
- ✅ Простой и понятный код
- ✅ Правильное использование исключений для нереализованных методов
- ⚠️ Метод `meta` использует `getattr` для получения атрибутов класса (может быть неочевидно)

**Рекомендация:** Добавить абстрактные свойства `description` и `columns_metadata`.

---

## Метрики кода

| Модуль | Строк | Функций/Методов | Сложность | Оценка |
|--------|-------|----------------|-----------|--------|
| converter.py | 1033 | 24 | Высокая | 🔴 |
| parser.py | 915 | 22 | Высокая | 🟠 |
| api.py | 412 | 11 | Средняя | 🟠 |
| metadata.py | 286 | 13 | Средняя | 🟠 |
| handlers/base.py | 279 | 7 | Низкая | 🟢 |
| executor.py | 112 | 3 | Низкая | 🟢 |

---

## Рекомендации по приоритетам

### Немедленно (1-2 недели):
1. 🔴 Рефакторинг `api.py` - устранение DRY нарушений
2. 🔴 Разбиение `converter.py` на модули
3. 🟠 Оптимизация поиска колонок в `parser.py`

### Краткосрочно (1 месяц):
4. 🔴 Удаление глобального состояния (breaking change - требует обновления документации)
5. 🟠 Рефакторинг `DefaultMetadataProvider`
6. 🟠 Создание `AliasManager`

### Среднесрочно (2-3 месяца):
7. 🟡 Внедрение Pydantic для валидации
8. 🟡 Кэширование скомпилированных запросов
9. 🟡 Реорганизация структуры модулей

### Долгосрочно (3+ месяца):
10. 🟡 Внедрение паттернов Builder/Factory
11. 🟡 Улучшение системы логирования
12. 🟡 Метрики и мониторинг производительности

---

## Оценка технического долга

**Общая оценка:** 🟠 Средний уровень технического долга

**Критические проблемы:** 3  
**Важные проблемы:** 3  
**Рекомендации:** 4

**Общее время на устранение критических проблем:** ~25-35 часов  
**Общее время на полный рефакторинг:** ~50-70 часов

---

## Заключение

Проект UMA имеет солидную архитектурную основу и хорошую типизацию, но страдает от:
1. Нарушений принципа DRY в критических местах
2. Использования глобального состояния
3. Файлов-монолитов, нарушающих SRP
4. Неэффективных алгоритмов поиска в парсере

Приоритетом должно стать устранение критических проблем (DRY, глобальное состояние, разбиение converter.py), что займет около 25-35 часов работы. Это значительно улучшит поддерживаемость и тестируемость кода.

---

**Автор:** Code Review System  
**Дата:** 2026-01-13
